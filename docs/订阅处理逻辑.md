 ---
  阶段一：创建 active 订阅（不用微信 API）
     
  前端 wx.requestSubscribeMessage() 弹窗
    → 用户点击允许
    → reportResult() → POST /api/notification/subscribe
    → notification.py:81-88 写入 DB，status='active'

  这一步纯前端 + 后端入库，不需要后端调微信 API。微信的授权结果由 wx.requestSubscribeMessage 直接返回给前端。

  ---
  阶段二：消费 active 订阅（调微信 API 发消息）

  notification.py 事件触发
    → 查到 status='active' 的订阅记录
    → client.send_approval_notice() / send_result_notice()
    → self.send()                          ← wx_subscribe.py:67
    → POST /cgi-bin/message/subscribe/send ← wx_subscribe.py:93（微信 API）
    → 成功 → sub.status = 'used'           ← notification.py:157/204/238

  核心代码在 wx_subscribe.py:67-107，分两步：

  ① 获取 access_token（wx_subscribe.py:34-61）— 缓存 2 小时，提前 5 分钟刷新：
  GET https://api.weixin.qq.com/cgi-bin/token?grant_type=client_credential&appid=...&secret=...

  ② 发送订阅消息（wx_subscribe.py:67-107）：
  POST https://api.weixin.qq.com/cgi-bin/message/subscribe/send?access_token=...
  Body: {touser, template_id, data, page, miniprogram_state: 'formal'}

  ---
  完整数据流

  前端授权 → DB: active
                  ↓
          notification.py 事件触发
                  ↓
  阶段二：消费 active 订阅（调微信 API 发消息）

  notification.py 事件触发
    → 查到 status='active' 的订阅记录
    → client.send_approval_notice() / send_result_notice()
    → self.send()                          ← wx_subscribe.py:67
    → POST /cgi-bin/message/subscribe/send ← wx_subscribe.py:93（微信 API）
    → 成功 → sub.status = 'used'           ← notification.py:157/204/238

  核心代码在 wx_subscribe.py:67-107，分两步：

  ① 获取 access_token（wx_subscribe.py:34-61）— 缓存 2 小时，提前 5 分钟刷新：
  GET https://api.weixin.qq.com/cgi-bin/token?grant_type=client_credential&appid=...&secret=...

  ② 发送订阅消息（wx_subscribe.py:67-107）：
  POST https://api.weixin.qq.com/cgi-bin/message/subscribe/send?access_token=...
  Body: {touser, template_id, data, page, miniprogram_state: 'formal'}

  ---
  完整数据流

  前端授权 → DB: active
                  ↓
          notification.py 事件触发
                  ↓
          查到 active 订阅
                  ↓
          wx_subscribe.py:send()
            → GET /token       (微信 API)
            → POST /send       (微信 API)
                  ↓
          成功 → DB: status 改为 'used'
          失败 → 静默丢弃，不影响审批流程
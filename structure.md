访客登记系统v1/
  ├── backend/                    # Python Flask 后端 (16 文件)
  │   ├── app.py                  # 应用入口 + 定时任务 + 管理后台路由
  │   ├── config.py               # 环境切换 (development/production)
  │   ├── requirements.txt        # Python 依赖列表
  │   ├── models/                 # SQLAlchemy 数据模型
  │   │   ├── visitor.py          # 访客 + 同行人 (含AES加解密)
  │   │   ├── approver.py         # 审批人 (含token录入机制)
  │   │   ├── department.py       # 部门表
  │   │   └── admin.py            # 管理员 + 系统配置 + 审批记录
  │   ├── routes/                 # API 路由 (6 蓝图)
  │   │   ├── auth.py             # 微信登录 / 管理员登录 / JWT认证
  │   │   ├── visitor.py          # 访客登记 / 状态查询 / 凭证获取
  │   │   ├── approver.py         # 审批人录入 / 待审批 / 审批操作 / 历史
  │   │   ├── admin.py            # 数据管理 / Excel导出 / 密码修改
  │   │   └── system.py           # 系统配置读写
  │   ├── services/               # 业务逻辑
  │   │   ├── crypto.py           # AES-256-CBC加解密 + bcrypt + 数据脱敏
  │   │   ├── approval.py         # 二级审批流程引擎
  │   │   └── export.py           # Excel导出 + 密码保护
  │   ├── utils/validators.py     # 身份证/手机号/车牌号/密码验证
  │   └── migrations/init.sql     # 数据库初始化SQL
  ├── miniprogram/                # 微信小程序 (25 文件)
  │   ├── app.js/json/wxss        # 全局配置
  │   ├── pages/                  # 7 个页面
  │   │   ├── index/              # 首页 (欢迎弹窗/滚动图片/快捷入口)
  │   │   ├── notice/             # 访客告知书 (勾选同意后进入登记)
  │   │   ├── register/           # 登记表单 (完整字段/同行人/车牌)
  │   │   ├── result/             # 审批结果+通行凭证展示
  │   │   ├── approval-list/      # 待审批列表 (审批人)
  │   │   ├── approval-detail/    # 审批详情+审批操作
  │   │   └── approval-history/   # 审批记录
  │   ├── components/             # 车牌号输入 / 同行人表单组件
  │   └── utils/                  # API封装 / 常量
  ├── admin/                      # Web管理后台
  │   ├── login.html              # 管理员登录页
  │   └── index.html              # 管理面板 (访客/审批/配置/密码)
  ├── docs/                       # 部署文档
  │   ├── deployment-test.md      # 测试环境部署步骤
  │   └── deployment-prod.md      # 生产环境部署步骤
  └── README.md

  
  ┌─────────────────────┬────────────────────────────────────────────────────────┐
  │       需求点        │                        实现方式                        │
  ├─────────────────────┼────────────────────────────────────────────────────────┤
  │ 身份证号/手机号加密 │ AES-256-CBC 加密存储，bcrypt 密码哈希                  │
  ├─────────────────────┼────────────────────────────────────────────────────────┤
  │ 部门下拉框          │ 数据库 departments 表 + API 提供选项                   │
  ├─────────────────────┼────────────────────────────────────────────────────────┤
  │ 同行人添加          │ visitor_count > 1 时动态添加同行人表单                 │
  ├─────────────────────┼────────────────────────────────────────────────────────┤
  │ 车牌号输入          │ 省份选择→城市字母→号码输入的组装式组件，自动识别新能源 │
  ├─────────────────────┼────────────────────────────────────────────────────────┤
  │ 二级审批            │ level1 按部门匹配 → level2 全局唯一，同部门跳过二级    │
  ├─────────────────────┼────────────────────────────────────────────────────────┤
  │ 会话2小时过期       │ APScheduler 每10分钟清理过期会话                       │
  ├─────────────────────┼────────────────────────────────────────────────────────┤
  │ Excel导出+密码      │ openpyxl + workbook security 密码保护                  │
  ├─────────────────────┼────────────────────────────────────────────────────────┤
  │ 数据脱敏            │ 身份证中间10位*，手机号中间4位*                        │
  ├─────────────────────┼────────────────────────────────────────────────────────┤
  │ 管理后台            │ 独立 Web 页面，不出现于小程序界面                      │
  ├─────────────────────┼────────────────────────────────────────────────────────┤
  │ 环境切换            │ ENV 环境变量控制 development/production                │
  └─────────────────────┴────────────────────────────────────────────────────────┘
